# 24dce030-AWDF
Project Name: MedCare Plus — Hospital Appointment Management System
Frontend Setup: Explains navigating to frontend, running npm install, and starting with npm run dev (http://localhost:5173).
Backend Setup: Explains navigating to backend, running npm install, and starting with npm start (http://localhost:5005).
MongoDB Setup: Explains installing MongoDB Community Server locally and setting the connection parameters.
Required Environment Variables: Documents the PORT and MONGO_URI configuration.
